/**
 * Created by zhaofs on 2017/11/2.
 * 默认实现支持如直接打开网页等
 */
import BaseHandler from "../../handler/BaseHandler";
import htmlUtils from "../../utils/HtmlUtils";

export default class extends BaseHandler {

    /**
     * 用iframe进行展示
     * @param args
     * {src:www.chrhc.com,type:[relative|absolute]-(默认 relative)}
     */
    iframeWrap(args) {
        if (args.src) {
            return this.render({tpl: require("./tpl/iframeWrap.html")}).then(function () {
                $('.content iframe').attr('src', args.src);
                htmlUtils.autoSizeIframe();
            });
        } else {
            throw new Error("参数无效，请传递如{src:www.chrhc.com,type:[relative|absolute]-(默认 relative)}");
        }

    }

    /**
     * 在新窗口中打开请求
     * @param args
     */
    openWin(args) {
        window.open(args.src)
    }

    encode(){

    }
};

